const todoInput = document.querySelector('#newtodo');
const todosListEl = document.querySelector('#todos-list');
const form = document.querySelector('#todoform');
const todo = 0;

export {
  todoInput, todosListEl, form, todo,
};
